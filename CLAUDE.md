# 開発ルール（Claude Code 向け）— www.harmonyhorizon.space

このリポジトリは **www.harmonyhorizon.space**（HARU の英語レッスン）。
`app.harmonyhorizon.space` は別リポジトリ（`harmony-horizon`）なので
取り違えないこと。実際に一度、別のリポジトリに作ってしまった。

## 触る前に

- 4ページ（`index` / `programs` / `student-voices` / `contact`）は
  **生成物**。直すのは `content/pages/*.html` と `tools/build-site.py`。
  ルートの `.html` を直接編集しても次のビルドで消える
- 直したら必ず `python3 tools/build-site.py`
- `eatout` / `immigration` / `self-expression` / `thank-you` は
  別系統（旧 `style.css` / `main.js`）。触らない

## 活字の下限（2026-08-27 必須ルール）

読者は **英語を学び直す 40〜60代**。50代の読者から
「全体的に文字が50代向けではない」「小さすぎて読めない」と
指摘を受けた。実際に測ると 13px 以下が10種類あった。

| 種類 | 下限 |
|---|---|
| **読む文章**（本文・注記・説明・引用・写真の説明・フォームの説明） | **16px** |
| **押すもの**（ボタン・ナビ・リンク・メール・フォームの見出し） | **16px** |
| **印**（番号・イニシャル・短いラベル） | **12px** |

- **タップ領域 44px を確保しても、字が 14px では押す前に読めない**
- 「印」と「文」を取り違えない。**声に出して読む長さがあるなら文**。
  機械的には15文字以上。引用の上の英文一行を `eyebrow` と同じ
  「印」だと思って 12px で組み、読めなくなった
- 密度が欲しいときは**余白**で作る。文字を下げない
- **寸法を変えたら、最初の一画面を測り直す**（下記）

## 最初の一画面（2026-08-27 必須ルール）

実機の Safari は下に操作バーが出るので、**iPhone 14 で見えているのは
852px ではなく 659px**。ここを 852px で確認していたせいで、
携帯の最初の画面にボタンが一度も出ていなかった。

- 高さの計算は `100vh` ではなく **`100svh`**
- ヒーロー写真の高さは「画面の幅」ではなく**「残っている高さ」**から決める
  （`--hero-band` / `--hero-text` を参照）
- **受け入れ条件：393×659 と 430×690 で「ボタンの下端 ≤ 画面の高さ」**

## 文言（2026-08-27 必須ルール）

- **本文で屋号を名乗らない。** 「Harmony Horizon」が出てよいのは
  ヘッダー・脚注の署名と `<title>` だけ
- **肩書きを書かない**（「主宰」など）
- **リード文を体言止めで終わらせない。** 「〜を。」で止めると文が
  終わらず、気取って見える。主語と述語のある文で書く。
  体言止めは見出しだけ
- 節を消す・並べ替えるときは、**その前後の文を通しで読み直す**。
  帯を1つ外したら、ヒーローの導入文と次の節の書き出しが
  一字一句同じだったことが表に出た

## app.harmonyhorizon.space は公開しない（2026-08-27 必須ルール）

**有料の中身なので、表のサイトから絶対にリンクしない**（HARU様の指示）。

- www のどのページからも `app.harmonyhorizon.space` へリンクしない
- 販売の導線は www 側の `/eatout/` `/immigration/` の販売LPだけ
- アプリ側の各ページには `<meta name="robots" content="noindex, nofollow">`
  を入れてある。**robots.txt では止めない。** 巡回を止めると
  noindex 自体が読まれず、他所からのリンクだけで索引に載ることがある

## 太さと色（2026-08-27 必須ルール）

- **自前ホストに無い太さを指定しない。** ブラウザは輪郭を太らせた
  合成太字（偽ボールド）で描く。和文は画数が多いので線がつぶれ、
  それだけで安っぽく見える。実測で販売LPの和文 102 か所のうち
  **61 か所**が合成太字だった。HARU様の「素人っぽい」の一番の正体
  - Zen Kaku Gothic New に **600 は無い**（300/400/500/700/900）
  - 可変書体（Inter・Playfair Display）は 1 ファイルで 100〜900 を持つ。
    `font-weight: 400` と `500` に分けて宣言すると**その太さに固定され**、
    700 を求めた時に合成太字になる。`font-weight: 100 900;` と宣言する
  - `node tools/mobile-audit.js` が検出する
- **差し色を本文の代わりに使わない。** 番号・チェック・矢印まで銅色に
  すると、1ページに銅が 40 か所（トップページは 16 か所）出る。
  静かな札は灰（`--stone-deep`）。銅は本当に強調したい所だけ
- **文字の濃さは1つに揃える。** #1F1B17 / #221E19 / #2C2620 が混在して
  いた。見分けはつかないのに揃っていないだけ。文字は `--ink` に一本化
- 白いカードの上は紙の上より対比が下がる。`--stone-deep` は
  **白の上で 4.5:1** になる濃さにする（#6E675A）

## 揃え方（2026-08-27 必須ルール）

- **読む文章（2行以上になりうるもの）は必ず左揃え。**
  日本語を中央に置くと左右どちらの端もそろわず、のこぎり状になり、
  どの画面幅でも「1〜3文字だけの行」が出る。
  HARU様の言葉：「基本的にぐちゃぐちゃしてて読みにくいです」
- 中央に置いてよいのは、**折り返さない短いもの**だけ
  （値札の金額、ボタンの中身、脚注の1行、札）
- 節ごと `style="text-align:center"` で包まない。
  中の本文・箇条・見出しが全部巻き添えになる
- 札や矢印の列も `justify-content: center` をやめる。
  折り返した2行目だけ中央に寄って階段状に見える
- **一つの段落に大きさ違いを3つ入れない。** 差は大きさではなく色か余白で
- 孤立行の直し方: 1〜2文の短い塊は読点で `<br>`。
  **3文以上の長い塊には入れない**（狭い画面で短い行が増える）。
  320px で見出しが収まらない時は `@media (max-width: 380px)` で
  見出しだけ一段下げる。本文の 16px は下げない

- **横並びは `justify-content: space-between` だけに任せない。**
  中身が入りきらない時は詰まって触れる。実測で 320/360px の
  ヘッダーはロゴとボタンの間隔が **0px** だった。必ず `gap` を書く
- `.btn` は `inline-flex` に `gap` がある。中の文字を `<span>` で
  分けると別々の項目になり、「¥1,980 で始める」のように隙間が空く。
  ひとつの `<span>` にまとめる
- **`:hover` は `@media (hover: hover)` で囲む。**
  そうしないと iOS で一度触れた色が貼り付いたまま残る

## 写真（2026-08-27 必須ルール）

- **携帯と卓上で別の画像を出す**（`<picture>` の `media`）。
  横 2048×1152 を携帯の縦長枠で切ると、原寸で横 544px 分しか見えず、
  耳は写るのに口が画面の外へ出た
- **切ってはいけない点を px で決めてから**、枠ごとに入るか計算する
- **寄りすぎない。** 顔を大きく切り出すと、イヤホンが拡大されて
  作り物であることが目につく。引いて部屋ごと見せる
- 素材集の1枚を埋め草に使わない。**「この写真は何を伝えるか」に
  一言で答えられないなら置かない**
- 意味の合わない場所に無理に残さない。合う場所へ移すか、
  その枠を文字だけの版にする
- **ヒーローの写真は、画面幅で「帯」と「全面」を切り替える。**
  `height: min(100vw*0.62, 260px)` のように px で上限を固定すると、
  卓上（1440px）で 5.5:1 の細長い隙間になり、人物の頭が切れる。
  `@media (min-width: 900px)` で `inset: 0; height: auto` に戻し、
  覆いも縦の階調（携帯）から横の階調（卓上）へ切り替える
- **主役が「文字を置きたい側」に写っていたら、まず左右反転を試す。**
  `transform: scaleX(-1)`。読める看板がある写真は、その矩形だけ
  元の向きに戻して焼き込む（追補4 §154 に手順）
- **暗くする側にピントの合った人物を置かない。** 覆った側は沈むので、
  ぼけたほうが明るい側に残ると「見えている顔がぼけている」状態になる。
  原寸で両方の顔を切り出して、どちらがくっきりしているか先に確かめる
- **全面に敷く写真は 2048px 幅で書き出す。** 1200px を 1440px の枠に
  伸ばすと粗く見える（HARU様の指摘 2026-08-27）。webp で 150KB 前後
- **写真に重ねる文字は、必ず写真の上で実測する**（`tools/hero-contrast.js`）。
  見出しの中の強調に濃い色（`--gold` #9C5C33）を使うと 1.4:1 しか出ない。
  写真の上では明るいほう（`--gold-pale` #E9B98C）を使う

## 公開前に必ず流す

```bash
python3 tools/build-site.py
bash    tools/subset-jp-fonts.sh    # 文言を変えたら必ず
python3 tools/check-jp-fonts.py     # 和文書体の抜け
node    tools/mobile-audit.js       # 横溢れ・44px・16px・中央揃え・孤立行・第三者通信
                                    # **9ページ全部**を見る。2026-08-27 まで
                                    # 販売LP2枚が一覧から漏れており、
                                    # 売っているページを一度も測っていなかった
node    tools/hero-contrast.js      # 写真の上の文字の対比（トップページ）
node    tools/hero-contrast.js http://127.0.0.1:8802/eatout/index.html
node    tools/hero-contrast.js http://127.0.0.1:8802/immigration/index.html
```

**目視で終わらせない。** ここに挙げた失敗は全部、
目で見たときは「きれいに見えていた」もの。

## ブランチとマージ

作業は `claude/redesign-blueprint`。**`main` に入って初めて公開される**
（Vercel）。区切りごとに「main にマージしますか？」と必ず聞く。
セッションが終わる前に未マージの変更が残っていないか確認する。

## 意匠の出典

`harmony-horizon` リポジトリの
`docs/contractor-website-blueprint/`（追補4・追補5）。
**新しく何かを組む前に、そこの README 冒頭「何より先に読むこと」を読む。**
